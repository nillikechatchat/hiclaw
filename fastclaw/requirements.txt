# fastclaw - Lightweight Python AI Agent Worker

# Core dependencies
aiohttp>=3.9.0          # Async HTTP client for Higress API calls
matrix-nio>=0.23.0      # Matrix client library (async)
aiomonitor>=0.7.0       # Async monitoring and debugging
pydantic>=2.0.0         # Data validation and settings management
python-dotenv>=1.0.0    # Environment variable management

# Optional: LLM SDKs (user can choose)
# anthropic>=0.18.0     # Claude SDK (if using claude sdk option)
# openai>=1.0.0         # OpenAI SDK (if using openai sdk option)

# Optional: MinIO client for direct file operations
# minio>=7.2.0          # MinIO Python client

# Optional: Health check and monitoring
# prometheus-client>=0.19.0  # Prometheus metrics
